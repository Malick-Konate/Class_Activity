#spring:
#  config:
#    activate:
#      on-profile: dev
#  h2:
#    console:
#      enabled: true
#      path: /h2-console
#      settings:
#        web-allow-others: true
#
#  datasource:
#    url: jdbc:h2:mem:handyman;DB_CLOSE_DELAY=-1;NON_KEYWORDS=YEAR,VALUE
#    driver-class-name: org.h2.Driver
#    username: sa
#    password:
#
#  jpa:
#    show-sql: true
#    defer-datasource-initialization: true
#    hibernate:
#      ddl-auto: create-drop
#    properties:
#      hibernate:
#
#        id:
#          db_structure_naming_strategy: single
#
#  sql:
#    init:
#      platform: h2
#      mode: always
#      schema-locations: classpath:schema.sql
#      data-locations: classpath:data.sql


#-----------------------------------------------------------------------------------------------------------------------

# Default Spring Profile
server:
  error:
    include-message: always

logging:
  level:
    root: INFO
    com.konate: DEBUG
    org.hibernate.SQL: DEBUG
    org.hibernate.type.descriptor.sql.BasicBinder: TRACE

---

# for docker

spring:
    config:
      activate:
        on-profile: docker


    datasource:
      url: jdbc:mysql://mysql1/handyman
      username: user
      password: pwd

    jpa:
      show-sql: true
      hibernate:
        ddl-auto: none   #use create to allow jpa to auto-generate from Entity classes; use none to use schema-h2.sql

      defer-datasource-initialization: true

    sql:
      init:
        platform: mysql
        mode: always
        schema-locations: classpath:schema.sql
        data-locations: classpath:data.sql

server.port: 8080

spring.datasource.hikari.initializationFailTimeout: 60000



---
spring:
  config:
    activate:
      on-profile: test

  datasource:
    url: jdbc:h2:mem:testdb
    driver-class-name: org.h2.Driver
    username: sa
    password:

  jpa:
    database-platform: org.hibernate.dialect.H2Dialect
    hibernate:
      ddl-auto: create-drop
    show-sql: false

  sql:
    init:
      mode: never


